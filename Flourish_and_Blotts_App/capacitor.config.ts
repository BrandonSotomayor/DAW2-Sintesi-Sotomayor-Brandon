import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Flourish_and_Blotts',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
